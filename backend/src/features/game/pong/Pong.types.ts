export enum Actions {
    REQUEST_STATE,
    MOVE_UP,
    MOVE_DOWN,
    SET_READY,
}

export const PossibleActions: Actions[] = [
    Actions.REQUEST_STATE,
    Actions.MOVE_UP,
    Actions.MOVE_DOWN,
    Actions.SET_READY,
];
