export enum Actions {
    AUTH,
    REQUEST_STATE,
    MOVE_UP,
    MOVE_DOWN,
    SET_READY,
}

export const PossibleActions: Actions[] = [
    Actions.AUTH,
    Actions.REQUEST_STATE,
    Actions.MOVE_UP,
    Actions.MOVE_DOWN,
    Actions.SET_READY,
];
